{
  "name": "open-tender-agent",
  "version": "0.1.0",
  "description": "Open-source TED-only Bun + TypeScript lead discovery and outreach generator",
  "type": "module",
  "main": "src/interfaces/cli.ts",
  "bin": {
    "open-tender-agent": "src/interfaces/cli.ts"
  },
  "scripts": {
    "dev": "bun run src/interfaces/cli.ts run --dry-run --verbose",
    "run": "bun run src/interfaces/cli.ts run",
    "wizard": "bun run src/interfaces/cli.ts wizard",
    "check": "bun test",
    "test": "bun test"
  },
  "license": "MIT",
  "dependencies": {
    "@clack/prompts": "^0.9.1",
    "chalk": "^5.3.0",
    "dotenv": "^16.4.5",
    "yaml": "^2.6.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.7.2"
  }
}
